import { ComponentInterface } from '../../../../stencil-public-runtime';
import ColumnEventManager from './ColumnEventManager';
export declare class CalendarColumnView implements ComponentInterface {
  elm: HTMLElement;
  events: any[];
  view: string;
  days: number;
  currentTime: Date;
  contextDate: Date;
  dateRange: any;
  singleDayEvents: any;
  manager: ColumnEventManager;
  componentWillLoad(): Promise<void>;
  componentDidLoad(): Promise<void>;
  renderHeader(): any[];
  renderBackgroundColumns(): any[];
  renderBodyBackground(): any;
  renderScale(): any;
  renderEvents(): any;
  getDatePercent(): number;
  getTimePercent(time: any, forDay?: any): number;
  render(): any;
}
