import CalendarEvent from './CalendarEvent';
/**
 * Calculates start and end dates of month in a calendar.
 * @param date Context date for which range need to calculated.
 * @param weekStartsOn A numeric value. The index of the first day of the week (0 - Sunday)
 */
export declare function calculateMonthRange(date: any, weekStartsOn?: 0 | 1 | 2 | 3 | 4 | 5 | 6): {
  startDate: Date;
  endDate: Date;
  totalDays: number;
};
export declare function calculateWeekRange(date: any, weekStartsOn?: 0 | 1 | 2 | 3 | 4 | 5 | 6): {
  startDate: Date;
  endDate: Date;
  totalDays: number;
};
export declare class EventNode {
  event: CalendarEvent;
  width: number;
  constructor(event: any);
}
export declare class EventBucket {
  #private;
  eventNodes: EventNode[];
  columns: EventNode[][];
  constructor();
  insert(node: EventNode): void;
  process(): void;
}
