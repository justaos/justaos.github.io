import { ComponentInterface } from '../../../stencil-public-runtime';
/**
 * @name Calendar
 * @description The calendar component is used to display information in a daily, weekly, monthly, or category view.
 * @img /assets/img/calendar.png
 */
export declare class Calendar implements ComponentInterface {
  elm: HTMLElement;
  events: any[];
  availableViews: any;
  view: string;
  /**
   * Show loader.
   */
  showLoader: boolean;
  timezone: any;
  currentTime: any;
  currentView: any;
  contextDate: any;
  componentWillLoad(): Promise<void>;
  renderCalendarView(): any;
  renderHeader(): any;
  render(): any;
}
